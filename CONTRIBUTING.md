# contributing

if u wanna send a puww wequest to `yt-defuwuck`, keep it tight and usefuw uwu

## keep each moduwe scoped
- `list.txt` is da Shorts moduwe
- `comments.txt` is da comments moduwe
- `playables.txt` is da Playables moduwe
- `subscriptions.txt` is da subscriptions moduwe

don’t dump unwewated bwocks into da wwong fiwe just because it was convenient.

## don’t touch da metadata headew
*Metadata = Title, Version, Last modified, Expires, Homepage, and License at da top of each wist.*

Onwy maintainers shouwd update dat stuff. Metadata needs to stay centwawized so vewsions and wewease info don’t get messy.

## use da fuww subdomain
Don’t stawt a fiwtew with `youtube.com`; use `www.youtube.com` ow `m.youtube.com` instead.
Dis keeps da bwocks out of pwaces wike `studio.youtube.com`.

## keep sewectows as simpwe and specific as possibwe
Don’t weave extwa cwasses in a fiwtew if dey don’t hewp. Anchow to stabwe text, stwuctuwe, ow URLs when dat fits da moduwe.

## make bugs easy to wepwoduce
if something slipped thwough, incwude:
- whethew u wewe on desktop ow mobiwe
- da bwowsew
- da extension
- da exact URL
- a scweenshot if it hewps

## keep da pwoject opinionated
`yt-defuwuck` is not twying to be a neutwaw catch-aww wist. Changes shouwd make YouTube mowe usabwe in da same ovewaww spiwit as da existing moduwes.
